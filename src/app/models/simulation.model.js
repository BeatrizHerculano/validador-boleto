class Validate {
    constructor(isValid, value, expirationDate, digits) {
        this.isValid = isValid;
        this.value = value;
        this.expirationDate = expirationDate;
        this.digits = digits;
    }
}
module.exports = Validate;
